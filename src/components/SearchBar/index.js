import React from 'react';

const SearchBar = () => (
  <div>
    <h1 className="title">Github</h1>
    <form>
      <input
        type="text"
        className="header-input"
      />
    </form>
  </div>
);

export default SearchBar;
